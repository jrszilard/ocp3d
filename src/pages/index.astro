---
import Base from "../layouts/Base.astro";
import PartCard from "../components/PartCard.astro";
import { allParts } from "../lib/catalog";
import { ClipboardList, Search, Ruler, Printer, CarFront } from "@lucide/astro";

const parts = allParts();
const featured = parts.slice(0, 3);
---
<Base title="The Ministry of Discontinued Parts: We remake the car parts nobody sells anymore">
  <div class="wrap hero">
    <div>
      <h1>We remake the car parts <span class="mk">nobody sells anymore.</span></h1>
      <p class="sub">
        3D printed faithful reproductions of hard-to-find car parts.
      </p>
      <div class="cta-row">
        <a class="btn primary" href="/registry/">Browse the parts</a>
        <a class="btn ghost" href="/request/">Request a part we don’t have</a>
      </div>
    </div>
    <div class="badge-holder">
      <div class="evidence-stack">
        <figure>
          <img src="/images/hero/measured.jpg" alt="The original OEM sun visor clip on the measuring bench, caliper scale alongside" width="1280" height="960" fetchpriority="high" />
          <figcaption>Measured · the original, 18 dims on record</figcaption>
        </figure>
        <figure>
          <img src="/images/parts/sun-visor-clip.jpg" alt="The reproduction sun visor clip, modeled from those measurements — neutral resin" width="1024" height="768" fetchpriority="high" />
          <figcaption>Reproduced · neutral resin, made to order</figcaption>
        </figure>
      </div>
    </div>
  </div>

  <section id="how">
    <div class="wrap">
      <div class="sec-head"><h2>How it works</h2><div class="rule"></div></div>
      <div class="steps">
        <div class="step"><ClipboardList class="step-icon" size={28} stroke-width={1.6} /><h3>You request a part</h3>
          <p>Tell us your car and the part you can’t buy anymore. It enters the catalog with its full OEM paper trail.</p></div>
        <div class="step"><Search class="step-icon" size={28} stroke-width={1.6} /><h3>We research it</h3>
          <p>We trace every part number it ever wore and draft the shape. Drafts are always labeled drafts.</p></div>
        <div class="step"><Ruler class="step-icon" size={28} stroke-width={1.6} /><h3>We measure an original</h3>
          <p>A surviving part meets the calipers: yours, a donor’s, or one we source. Every critical dimension, published.</p></div>
        <div class="step"><Printer class="step-icon" size={28} stroke-width={1.6} /><h3>Made to order</h3>
          <p>You order; we print, inspect, and ship yours. It fits, or we remake it free. Group runs lower the price when demand pools.</p></div>
        <div class="step"><CarFront class="step-icon" size={28} stroke-width={1.6} /><h3>Fitted on a real car</h3>
          <p>The part goes on an actual car and gets confirmed: car, year, and material on the record. Only then does it wear the badge.</p></div>
      </div>
    </div>
  </section>

  <section id="reel">
    <div class="wrap">
      <div class="sec-head"><h2>From original to reproduction</h2><div class="rule"></div></div>
      <div class="reel">
        <figure>
          <img src="/images/process/original.jpg" alt="Close-up of the original 1980s OEM clip on the cutting mat" width="1280" height="960" loading="lazy" decoding="async" />
          <figcaption>01 · The original — survivor in hand</figcaption>
        </figure>
        <figure>
          <img src="/images/process/measurement.jpg" alt="Caliper jaw and bench ruler on the measuring bench, clip behind" width="1280" height="960" loading="lazy" decoding="async" />
          <figcaption>02 · The measurement — every dim on record</figcaption>
        </figure>
        <figure>
          <img src="/images/parts/sun-visor-clip.jpg" alt="The reproduction clip rendered in neutral resin" width="1024" height="768" loading="lazy" decoding="async" />
          <figcaption>03 · The reproduction — printed to order</figcaption>
        </figure>
        <figure class="reserved">
          <div class="reserved-plate" role="img" aria-label="Reserved frame: no part has been fitted on a car yet">
            <span>This frame reserved<br />for the first fit on a real car</span>
          </div>
          <figcaption>04 · The first fit — <a href="/#register">yours?</a></figcaption>
        </figure>
      </div>
    </div>
  </section>

  <section>
    <div class="wrap">
      <div class="sec-head">
        <h2>Parts we’re bringing back</h2>
        <div class="rule"></div>
        <a href="/registry/">See all {parts.length} parts →</a>
      </div>
      <div class="cases">{featured.map((p) => <PartCard part={p} />)}</div>
    </div>
  </section>

  <section id="register">
    <div class="wrap register">
      <div class="join">
        <div class="eyebrow">The Register · free</div>
        <h2>Register with the Ministry.</h2>
        <p>
          Free, forever, and numbered — your registrant number is yours the moment you claim it.
          What it buys you, stated plainly: <b>The Ministry Gazette</b>, our bulletin, which ships
          only when a part actually moves forward. Never on a schedule. Voting on the registry is
          open to everyone, registered or not. The parts cost money. Registration never does.
        </p>
        <form class="join-row" id="join-form" method="post" action="/api/request">
          <input type="hidden" name="type" value="membership" />
          <input type="text" name="company" class="hp" tabindex="-1" autocomplete="off" aria-hidden="true" />
          <input type="email" name="email" required placeholder="you@garage.example" aria-label="Email address" />
          <button class="btn primary" type="submit">Claim a registrant number</button>
        </form>
        <p class="join-done" id="join-done" hidden>You’re on the register. The Gazette ships when a part moves — never for noise.</p>
        <small>
          We keep your address to send the Gazette, and for nothing else — never sold, never
          shared. Every issue carries a one-click way out, and you can
          <a href="/unsubscribe/">leave at any time</a>.
        </small>
      </div>
      <div class="m-card" role="img" aria-label="Sample registration card">
        <div class="mc-top">
          <span class="mc-title">MINISTRY OF DISCONTINUED PARTS</span>
          <span class="mc-no">REGISTRANT № 0001</span>
        </div>
        <dl>
          <dt>Chapter</dt><dd>A60 · Celica Supra</dd>
          <dt>Standing</dt><dd>Fitment Officer, Provisional</dd>
          <dt>Testimony</dt><dd>1 part measured (18 dims)</dd>
          <dt>Since</dt><dd>2026</dd>
        </dl>
        <div class="oath">“I solemnly swear to measure twice.”</div>
      </div>
    </div>
  </section>
</Base>

<script>
  const joinForm = document.getElementById("join-form") as HTMLFormElement;
  joinForm.addEventListener("submit", async (e) => {
    e.preventDefault();
    const btn = joinForm.querySelector("button") as HTMLButtonElement;
    btn.disabled = true;
    try {
      const res = await fetch(joinForm.action, {
        method: "POST",
        body: new FormData(joinForm),
        headers: { accept: "application/json" },
      });
      const data = await res.json();
      if (res.ok && data.ok) {
        joinForm.hidden = true;
        document.getElementById("join-done").hidden = false;
      } else if (data.unconfigured) {
        const email = (new FormData(joinForm).get("email") as string) ?? "";
        window.location.href = `mailto:requests@lostclipsociety.com?subject=${encodeURIComponent("Registration — Ministry of Discontinued Parts")}&body=${encodeURIComponent("Sign me up: " + email)}`;
      } else {
        btn.disabled = false;
      }
    } catch {
      btn.disabled = false;
    }
  });
</script>

<style>
  .hero { display: grid; grid-template-columns: 1.25fr 0.75fr; gap: var(--space-6); align-items: center; padding: var(--space-7) 24px 56px; }
  .hero h1 { font-size: var(--text-hero); line-height: 1.08; }
  /* Sublime is a highlighter, never a surface — this is its one job. */
  .hero h1 .mk { box-shadow: inset 0 -0.26em 0 color-mix(in srgb, var(--paint-sublime) 65%, transparent); }
  .sub { max-width: 56ch; color: var(--fg-dim); margin: 18px 0 26px; }
  .sub em { color: var(--fg); font-style: normal; border-bottom: 2px solid var(--action); }
  .cta-row { display: flex; gap: var(--space-3); flex-wrap: wrap; }
  .badge-holder { display: flex; justify-content: center; min-width: 0; }
  /* The hero evidence pair: measured (real photo of the original) over
     reproduced (neutral-resin render). Squared off, captioned in mono —
     evidence, not decoration. */
  .evidence-stack { display: grid; gap: var(--space-3); width: 100%; min-width: 0; }
  .evidence-stack figure {
    margin: 0; background: var(--panel);
    border: var(--border-hairline) solid var(--edge);
  }
  .evidence-stack img { display: block; width: 100%; height: auto; aspect-ratio: 4 / 3; object-fit: cover; }
  .evidence-stack figcaption {
    font-family: var(--font-mono); font-size: var(--text-xs);
    letter-spacing: var(--tracking-label); text-transform: uppercase;
    color: var(--panel-dim); padding: 8px 12px;
    border-top: var(--border-hairline) solid var(--panel-line);
  }
  @media (max-width: 820px) { .hero { grid-template-columns: 1fr; } .badge-holder { order: -1; } }

  /* The process reel: four frames, sprocket-holed — the filmstrip the method
     deserves. Last frame stays empty until a part is fitted on a real car. */
  .reel {
    display: grid; grid-template-columns: repeat(4, minmax(0, 1fr)); gap: var(--space-3);
    padding: var(--space-3) var(--space-3);
    background: var(--brand-charcoal);
    border-top: 14px solid var(--brand-charcoal);
    border-bottom: 14px solid var(--brand-charcoal);
    position: relative;
  }
  .reel::before, .reel::after {
    content: ""; position: absolute; left: 0; right: 0; height: 14px;
    background-image: radial-gradient(circle 3px, var(--bg) 98%, transparent 100%);
    background-size: 26px 14px; background-position: 7px 0; background-repeat: repeat-x;
  }
  .reel::before { top: -14px; }
  .reel::after { bottom: -14px; }
  .reel figure { margin: 0; min-width: 0; background: var(--panel); border: var(--border-hairline) solid var(--brand-charcoal-2); }
  .reel img { display: block; width: 100%; height: auto; aspect-ratio: 4 / 3; object-fit: cover; }
  .reel figcaption {
    font-family: var(--font-mono); font-size: var(--text-xs);
    letter-spacing: var(--tracking-label); text-transform: uppercase;
    color: var(--panel-dim); padding: 8px 10px;
    border-top: var(--border-hairline) solid var(--panel-line);
  }
  .reel figcaption a { color: var(--action); text-decoration: none; }
  .reel figcaption a:hover { text-decoration: underline; }
  .reserved-plate {
    aspect-ratio: 4 / 3; display: flex; align-items: center; justify-content: center;
    text-align: center; padding: 0 var(--space-3);
    font-family: var(--font-mono); font-size: var(--text-xs);
    letter-spacing: 0.05em; line-height: 1.6; color: var(--panel-dim);
    background:
      repeating-linear-gradient(0deg, transparent, transparent 23px, rgba(15, 128, 196, 0.10) 24px),
      repeating-linear-gradient(90deg, transparent, transparent 23px, rgba(15, 128, 196, 0.10) 24px),
      var(--panel);
  }
  @media (max-width: 820px) { .reel { grid-template-columns: repeat(2, minmax(0, 1fr)); } }

  .cases { display: grid; grid-template-columns: repeat(auto-fit, minmax(290px, 1fr)); gap: var(--space-4); }

  .steps { display: grid; grid-template-columns: repeat(auto-fit, minmax(180px, 1fr)); gap: 1px; background: var(--edge); border: var(--border-hairline) solid var(--edge); }
  .step { background: var(--bg-2); padding: var(--space-4) 18px; }
  .step-icon { color: var(--action); display: block; margin-bottom: 10px; }
  .step h3 { font-size: 1.02rem; margin: 8px 0 6px; }
  .step p { font-size: 0.82rem; color: var(--fg-dim); margin: 0; }

  .register { display: grid; grid-template-columns: 1fr 1fr; gap: var(--space-6); align-items: center; }
  @media (max-width: 820px) { .register { grid-template-columns: 1fr; } }
  .join h2 { font-size: 1.7rem; margin-bottom: 10px; }
  .join p { color: var(--fg-dim); max-width: 52ch; }
  .join-row { display: flex; gap: 10px; margin-top: 18px; flex-wrap: wrap; }
  .join-row[hidden] { display: none; }
  .join-row .hp { position: absolute; left: -9999px; width: 1px; height: 1px; overflow: hidden; }
  .join-done { font-family: var(--font-mono); font-size: 0.85rem; color: var(--status-measured); margin-top: 14px; }
  .join-row input { font-family: var(--font-mono); font-size: 0.85rem; padding: 12px 14px; min-width: 240px; background: var(--bg-2); border: var(--border-hairline) solid var(--edge); color: var(--fg); }
  .join small { display: block; margin-top: 10px; font-family: var(--font-mono); font-size: var(--text-xs); color: var(--fg-dim); }

  .m-card { background: linear-gradient(135deg, #efe7d3, #e4d9bd); color: #26221a; border: 1px solid #cfc2a0; padding: 22px 24px; max-width: 420px; box-shadow: var(--shadow-card); transform: rotate(-1.2deg); }
  .mc-top { display: flex; justify-content: space-between; align-items: center; border-bottom: 2px solid var(--brand-oxblood); padding-bottom: 10px; margin-bottom: 14px; }
  .mc-title { font-family: var(--font-display); font-size: 0.78rem; letter-spacing: 0.04em; }
  .mc-no { font-family: var(--font-mono); font-size: 0.8rem; color: var(--brand-oxblood); font-weight: 700; }
  .m-card dl { display: grid; grid-template-columns: auto 1fr; gap: 4px 16px; margin: 0; font-size: 0.8rem; }
  .m-card dt { font-family: var(--font-mono); font-size: 0.62rem; letter-spacing: var(--tracking-label); text-transform: uppercase; color: #8a8067; align-self: center; }
  .m-card dd { margin: 0; font-family: var(--font-mono); }
  .oath { margin-top: 14px; font-family: var(--font-display); font-style: italic; font-size: 0.86rem; color: #514a38; }
</style>
