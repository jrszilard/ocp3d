---
import type { PartState } from "../lib/catalog";
interface Props { state: PartState; label: string; safetyGated?: boolean }
const { state, label, safetyGated = false } = Astro.props;
---
<span class:list={["stamp", state, safetyGated && "safety"]}>{label}</span>

<style>
  /* The state stamp is semantic, not decorative. The journal may be atmospheric,
     but a state remains a literal evidence claim. */
  .stamp {
    font-family: var(--font-mono); font-weight: 600;
    letter-spacing: var(--t-eyebrow-tr); font-size: var(--t-eyebrow);
    border: 1px solid currentColor; padding: 5px 9px;
    display: inline-block; text-transform: uppercase; white-space: nowrap;
  }
  .requested   { color: var(--steel); }
  .development { color: var(--body); }
  .measured { color: var(--amber); }
  .fitted { color: var(--ion); }
  .safety { color: var(--signal); }
</style>
