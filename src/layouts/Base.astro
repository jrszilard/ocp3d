---
import "../styles/global.css";
import Mark from "../components/Mark.astro";
interface Props { title: string; description?: string }
const { title, description = "The Ministry of Discontinued Parts: reproduction parts for classic cars — measured from originals, made to order, fitted on real cars." } = Astro.props;
---
<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{title}</title>
    <meta name="description" content={description} />
    <link rel="icon" href="/favicon.svg" type="image/svg+xml" />
    <!-- The Ministry's slab, self-hosted (see public/fonts/README.md — NOT a CDN, on purpose).
         Preloaded because the wordmark and every heading above the fold are set in it. -->
    <link rel="preload" href="/fonts/bitter-var-latin.woff2" as="font" type="font/woff2" crossorigin />
  </head>
  <body>
    <header class="site">
      <div class="wrap bar">
        <a class="markrow" href="/">
          <span class="mini-crest"><Mark size={30} /></span>
          <span class="wordmark">MINISTRY OF <b>DISCONTINUED PARTS</b></span>
        </a>
        <nav class="main" aria-label="Primary">
          <a href="/registry/">Parts Catalog</a>
          <a href="/request/">Request a Part</a>
          <a href="/method/">How We Verify Fit</a>
          <a href="/about/">About</a>
        </nav>
        <a class="member-cta" href="/#register">Join Free</a>
      </div>
    </header>

    <main><slot /></main>

    <footer class="site">
      <div class="wrap cols">
        <div>
          <div class="wordmark" style="margin-bottom:8px">MINISTRY OF <b>DISCONTINUED PARTS</b></div>
          <p class="fine charter">
            Ministry charter, Article IV: we collect measurements, not data.
          </p>
          <p class="fine">
            Reproduction interior trim for cars the industry gave up on. Pre-2000, unbranded,
            non-safety-critical parts only. Every design is our own, modeled in-house from
            measured original parts — measurements published free, fit proven on real cars
            before any badge says so. This page is never NLA.
          </p>
        </div>
        <nav aria-label="Footer">
          <a href="/registry/">Catalog</a>
          <a href="/request/">Request a part</a>
          <a href="/method/">How we verify fit</a>
          <a href="/about/">About / the Charter</a>
          <a href="/styleguide/">Design system</a>
        </nav>
      </div>
      <div class="wrap">
        <p class="fine small">
          © 2026 The Ministry of Discontinued Parts. The Ministry accepts no responsibility for tears shed
          over crumbled OEM plastic. OEM part numbers appear for identification only; the
          Ministry is not affiliated with, endorsed by, or laughed at by any manufacturer. Anymore.
        </p>
      </div>
    </footer>
  </body>
</html>

<style>
  header.site { border-bottom: var(--border-hairline) solid var(--edge); }
  .bar { display: flex; align-items: center; gap: 18px; padding: var(--space-3) 0; }
  .markrow { display: flex; align-items: center; gap: 10px; text-decoration: none; }
  .mini-crest { display: flex; }
  .wordmark { font-family: var(--font-display); font-size: 1.02rem; letter-spacing: 0.04em; }
  .wordmark b { color: var(--action); }
  nav.main { margin-left: auto; display: flex; gap: var(--space-4); flex-wrap: wrap; }
  nav.main a {
    font-family: var(--font-mono); font-size: var(--text-xs); letter-spacing: 0.1em;
    text-transform: uppercase; text-decoration: none; color: var(--fg-dim);
    padding: 4px 0; border-bottom: 2px solid transparent;
  }
  nav.main a:hover { color: var(--fg); border-bottom-color: var(--accent); }
  .member-cta {
    font-family: var(--font-mono); font-size: var(--text-xs); letter-spacing: 0.08em;
    color: var(--action-fg); background: var(--accent);
    padding: 7px 12px; text-decoration: none;
    border: var(--border-hairline) solid var(--paint-signal-deep);
  }
  .member-cta:hover { background: var(--paint-signal-deep); }
  /* The footer rides Plum Crazy in both themes — the deep ground of the system. */
  footer.site { background: var(--footer-bg); color: var(--footer-fg); padding: var(--space-6) 0; margin-top: var(--space-6); }
  footer.site .wordmark b { color: var(--paint-sunflower); }
  .cols { display: flex; justify-content: space-between; gap: var(--space-5); flex-wrap: wrap; align-items: flex-start; }
  .fine { font-family: var(--font-mono); font-size: var(--text-xs); color: var(--footer-dim); max-width: 52ch; line-height: 1.8; }
  .fine.charter { color: var(--paint-sunflower); letter-spacing: 0.05em; text-transform: uppercase; font-size: 0.64rem; margin-bottom: 6px; }
  .fine.small { margin-top: var(--space-4); }
  footer nav { display: flex; gap: var(--space-3); flex-wrap: wrap; }
  footer nav a {
    font-family: var(--font-mono); font-size: var(--text-xs); letter-spacing: 0.1em;
    text-transform: uppercase; color: var(--footer-dim); text-decoration: none;
  }
  footer nav a:hover { color: var(--paint-sunflower); }
</style>
